{% if include.full_width %}<span class="image fit">{% endif %}
<img src="{{ "/images/" | prepend: site.base_url | append: include.image }}" alt="{{ include.alt }}" />
{% if include.full_width %}</span>{% endif %}
